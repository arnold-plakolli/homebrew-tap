# Homebrew Tap

Homebrew formulae for tools by Arnold Plakolli.

## Install

```bash
brew tap arnold-plakolli/tap
brew install qrstash
```

Or in one command:

```bash
brew install arnold-plakolli/tap/qrstash
```
